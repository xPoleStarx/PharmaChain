# ============================================
# PharmaChain Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ============================================
# Frontend Configuration (Vite)
# ============================================

# Enable real blockchain service (true) or use mock service (false)
# Default: false (uses localStorage-based mock service)
VITE_USE_REAL_BLOCKCHAIN=false

# Contract address (required if VITE_USE_REAL_BLOCKCHAIN=true)
# Get this address after running: npm run deploy:local
VITE_CONTRACT_ADDRESS=

# Provider URL for blockchain connection
# Default: http://localhost:8545 (Hardhat local node)
VITE_PROVIDER_URL=http://localhost:8545

# ============================================
# Hardhat Configuration (Optional)
# ============================================
# Uncomment and configure for testnet deployment

# Sepolia Testnet RPC URL (e.g., from Infura, Alchemy, or public RPC)
# SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# Private key for deployment account (NEVER commit this!)
# Must have testnet ETH for gas fees
# PRIVATE_KEY=your_private_key_here

# ============================================
# Notes
# ============================================
# 1. For local development:
#    - Set VITE_USE_REAL_BLOCKCHAIN=false (uses mock service)
#    - No contract address needed
#
# 2. For local Hardhat node:
#    - Start node: npm run node
#    - Deploy contract: npm run deploy:local
#    - Set VITE_USE_REAL_BLOCKCHAIN=true
#    - Set VITE_CONTRACT_ADDRESS to deployed address
#    - Set VITE_PROVIDER_URL=http://localhost:8545
#
# 3. For testnet deployment:
#    - Uncomment SEPOLIA_RPC_URL and PRIVATE_KEY
#    - Update hardhat.config.js to enable sepolia network
#    - Deploy: npx hardhat run scripts/deploy.js --network sepolia